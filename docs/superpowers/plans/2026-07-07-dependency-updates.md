# Dependency Updates (PR1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Update all dependencies within their semver (wanted) ranges to clear the `npm audit` findings (17 vulnerabilities, 8 high at inventory time), without any major-version bumps.

**Architecture:** No code changes expected — only `package-lock.json` (and `package.json` only if untouched by `npm update`, which respects existing ranges). Verification is the whole job: tests, build, lint must stay green on the updated tree.

**Tech Stack:** npm, Jest, TypeScript, ESLint.

**Spec:** `docs/superpowers/specs/2026-07-07-tech-debt-cleanup-design.md` (PR1 section)

## Global Constraints

- Branch: `chore/dependency-updates` (already exists, spec committed).
- No major-version updates. `npm update` only (wanted range). Do NOT run `npm audit fix --force`.
- Expected key bumps: discord.js 14.21.0 → 14.26.4, axios 1.11.0 → 1.18.1, prettier 3.6.2 → 3.9.4, typescript 5.8.3 → 5.9.3, typescript-eslint 8.35.1 → 8.63.0.
- Residual vulnerabilities that would need a major bump: record them in the PR body, do not fix now.
- Commit messages: subject ≤50 chars, no Claude footer / Co-Authored-By.
- Do NOT run repo-wide `npm run format` / fix format:check failures — that is PR3's job (prettier 3.9 will change output; formatting happens after this PR merges).

---

### Task 1: Update dependencies within wanted ranges and verify

**Files:**

- Modify: `package-lock.json` (regenerated by npm)
- Modify: `package.json` (only if npm changes it; ranges should stay as-is)

**Interfaces:**

- Consumes: nothing.
- Produces: updated dependency tree; later PRs (PR3 formatting) rely on prettier being 3.9.4 after this merges.

- [ ] **Step 1: Record the before state**

Run: `npm audit 2>&1 | tail -3 && npm ls discord.js axios prettier typescript`
Expected: audit reports 17 vulnerabilities (3 low, 6 moderate, 8 high); current versions match the Global Constraints "from" versions.

- [ ] **Step 2: Update within wanted ranges**

Run: `npm update`
Expected: `package-lock.json` changes; `npm ls discord.js axios prettier typescript` now shows discord.js@14.26.4, axios@1.18.1, prettier@3.9.4, typescript@5.9.3 (or newer patch within range).

- [ ] **Step 3: Re-check audit and record residuals**

Run: `npm audit 2>&1 | tail -6`
Expected: 0 vulnerabilities, or a small residual list. If residuals remain, run `npm audit` (full output) and copy each remaining advisory (package, severity, fix version) into your report — they go in the PR body as "requires major bump, deferred".

- [ ] **Step 4: Verify tests, build, lint**

Run: `npm test && npm run build && npm run lint`
Expected: all 168+ tests pass; `tsc` compiles with no errors; ESLint no issues. If discord.js or typescript minor bumps break compilation, STOP and report BLOCKED with the exact error — do not patch source code in this PR without controller sign-off.

- [ ] **Step 5: Confirm no unintended file changes**

Run: `git status --short`
Expected: only `package-lock.json` (and possibly `package.json`) modified. Nothing else.

- [ ] **Step 6: Commit**

```bash
git add package.json package-lock.json
git commit -m "chore: update deps within semver ranges" -m "Clears npm audit findings via wanted-range updates (discord.js
14.26.4, axios 1.18.1, prettier 3.9.4, typescript 5.9.3, and other
minors). No major bumps."
```

Note: if `package.json` is unchanged, `git add` it anyway is harmless; commit will include only what changed.
